# 排除 vendor 中的测试代码分析
# vendor/liquid_glass_widgets/test/ 使用 alchemist 包进行 golden 测试，
# 该包不是本项目的依赖，因此 CI analyze 会报错。
# 此处配置禁止对该目录进行分析。
analyzer:
  exclude:
    - "vendor/liquid_glass_widgets/test/**"
