/// 欲望空间：弹窗文案与节奏
///
/// ════════════════════════════════════════════════════════════════════════
///  ★ 想改弹窗里出现的文字？就改下面 kDesirePopupTexts 这个列表 ★
///
///  写法：每一条用单引号包起来，行尾加一个英文逗号。例如
///      '这是第一句',
///      '这是第二句',
///
///  · 想加多少条都行，弹窗会从里面随机取
///  · 想删就整行删掉
///  · 这里的内容 + 你在 App 里点「添加提醒」写的内容，会一起进随机池
///  · 如果两边都空着，就不会弹（不会报错）
/// ════════════════════════════════════════════════════════════════════════
library;

/// ★ 弹窗文字：把下面几行换成你自己的话 ★
///
/// 默认内容刻意保持中性，只是占位示例 —— 真正的文字请你自己写，
/// 因为这些话是说给你自己听的，别人写的不如你自己写的管用。
const List<String> kDesirePopupTexts = <String>[
  // ↓↓↓ 在这两个引号之间写你自己的话 ↓↓↓
  '是不是现实中也挺反差的呀',
  '在这里尽情释放欲望吧，没人发现的',
  '可以帮你记录并且把你了，只可以自己知道哦',
  // ↑↑↑ 上面几行都可以删掉或改写 ↑↑↑
];

// ─── 弹窗节奏（想调快 / 调慢改这里）──────────────────────────────────────────

/// 每隔多久弹出一个新的
const Duration kDesirePopupInterval = Duration(milliseconds: 700);

/// 每个弹窗在屏幕上停留多久后自动消失
const Duration kDesirePopupLifetime = Duration(seconds: 5);

/// 同时最多存在几个弹窗
///
/// 这个上限**不能去掉**：每个弹窗都是一个独立的 widget + 合成层，
/// 无节制地生成会迅速把渲染压垮（掉帧、发热，严重时 OOM 闪退）。
const int kDesirePopupMaxConcurrent = 12;

/// 单个弹窗最大宽度占屏幕宽度的比例
const double kDesirePopupWidthFactor = 0.66;
